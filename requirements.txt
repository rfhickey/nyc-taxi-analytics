# After installing, upgrade mashumaro past dbt-core's stale cap:
#   pip install --upgrade "mashumaro[msgpack]>=3.15"
# dbt-core pins mashumaro <3.15, but every mashumaro below 3.15 crashes dbt at
# startup on current Python 3.12 patch releases (UnserializableField:
# "schema" in JSONObjectSchema). mashumaro >=3.15 fixes the crash and works
# with dbt despite the declared cap. Pinning it here instead would make pip
# backtrack to dbt-core 1.8, so the override is a separate install step (see
# Makefile, CI workflow, and README quickstart).
dbt-duckdb>=1.9.0
requests>=2.31.0
